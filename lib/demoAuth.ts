/**
 * Tester / demo access has been fully removed for the production site.
 *
 * This stub remains only so existing imports keep compiling. It always
 * reports that a user is NOT a tester, so no free or demo grid access is
 * ever granted anywhere in the app.
 */
export function isGrid2DemoEmail(_email?: string | null): boolean {
  return false;
}
